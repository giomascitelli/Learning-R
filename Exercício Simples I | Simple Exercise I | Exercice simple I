# Exercício de fazer um código que soma os valores dentro de uma range e retorna eles.
# This is an exercise in which I made a code that sums the values in the given range and returns it.
# Exercice pour créer un code qui additionne les valeurs à l'intérieur d'une plage et les renvoie.

calc <- function(a,b) {
    res <- 0
    for (x in a:b) {
        res <- res + x
        }

    return (res)

    }

print(calc(4,7))

