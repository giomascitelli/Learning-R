# Exercício para criar um código que converte o valor que o usuário digitar em Litro para mL e mostrar o resultado.
# This is an exercise in which I created a code that takes an Liter amount as input, converts it to mL and outputs the result.
# Exercice visant à créer un code qui convertit la valeur saisie par l'utilisateur dans Litre en mL et affiche le résultat.

input <- readLines('stdin')

x <- input[1]
x <- as.integer(x)

print(x*1000)

