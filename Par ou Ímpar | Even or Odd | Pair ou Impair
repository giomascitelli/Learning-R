# O código retorna "par" se o número digitado pelo usuário for par e "ímpar" se for ímpar.
# The code returns "even" if the user inputs an even number and "odd" if he inputs an odd number.
# Le code renvoie "pair" si l'utilisateur saisit un nombre pair et "impair" s'il saisit un nombre impair.

x <- readLines('stdin')
num <- as.integer(x[1])

if (num %% 2 == 0) {
    print("even")
} else {
    print("odd")
}

